require 'spec_helper'

describe Mandrill::Template::Manager do
  it 'has a version number' do
    expect(Mandrill::Template::Manager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
